<!DOCTYPE html>
<html>
  <head>
    <style type="text/css">
    #gradation {
      background: -moz-linear-gradient(top, #e9f9e3, #FFF);
      background: -webkit-linear-gradient(top, #e9f9e3, #FFF);
      background: linear-gradient(to bottom, #e9f9e3, #FFF);
      /*グラデーション以外は省略*/
    }
    </style>
    <title>リスト</title>
    <!-- %input{name: "studentId", type: "hidden", value: } -->
  　<meta charset="UTF-8">
  </head>
  <body>
    <div id="gradation">
      <div>
        <h2>履修中の科目</h2>
      </div>
      <p>この中から、取り消す科目にチェックを入れてください</p>
      <ul class="list-group">
        <%# @course_teacher.each do |course_teacher| %>
<!--        <div class="input-group mb-3">-->
<!--          <div class="input-group-text">-->
<!--            <input type="checkbox" class="checkbox" aria-label="Checkbox for following text input">-->
<!--            <input type="hidden" class="courseTeacherId" value="<%#= course_teacher.id %>>">-->
<!--          </div>-->
<!--          <p type="text" class="form-control" aria-label="Text input with radio button">-->
            <%# course = @course.find(course_teacher.course_id)%>
            <%# teacher = @teacher.find(course_teacher.teacher_id)%>
<!--            <em><%#= course.course_code %></em>  |　<em><%#= course.course_name %></em>   |　<em><%#=teacher.teacher_name %></em>-->
<!--          </p>-->
<!--        </div>-->
        <%# end %>
        <% @course_list.each do |course| %>
        <div class="input-group mb-3">
          <div class="input-group-text">
            <input type="checkbox" class="checkbox" aria-label="Checkbox for following text input">
          </div>
          <p type="text" class="form-control" aria-label="Text input with radio button">
            <em><%= course.subcategories[0].courses[0].teachers%></em>
          </p>
        </div>
        <% end %>
      </ul>
      <br>
      <button type="button" id="confirm" class="btn btn-outline-success">確認へ</button>
    </div>
    <%= image_tag('forest1') %>
  </body>
</html>
